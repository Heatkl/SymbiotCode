using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NPCFast : NPCBehaviour
{
    [Header("Additional Settings")]

    public float Acceleration = 0f;
    public float maxSpeed = 0f;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
